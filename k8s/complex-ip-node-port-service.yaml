apiVersion: v1
kind: Service
metadata:
  name: complex-ip-node-port-service
  labels:
    component: ingress
spec:
  type: NodePort
  selector:
    component: ingress
    tier: web
  ports:
    - port: 8080
      targetPort: 8080